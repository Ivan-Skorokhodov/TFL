# RK2 (Скороходов Иван ИУ9-51Б, вариант 24)

### Здача 1

Язык SRS $ac \to ca, c \to bcb, bc \to cb$ над базисом $a^nca^nc$

Заметим, что:
- во всем слове у нас четное количество $a$
- количество $c$ ровно 2 шт и оно не изменяется
- в каждом блоке с $b$ у нас четное количество $b$
- разные блоки из $b$ (где в каждом четное количество) могут разделяться только блоками из $a$ (считаем, что $c$ внутри блока $b$ никак его не разбивает его на 2 отдельных блока)

Согласно второй лабораторной работе 2 построим ПКА, который покажет, что наш язык регулярный.
![ПКА](AFA.svg)

```
digraph AFA {
  rankdir=LR;
  node [shape = doublecircle]; 0; 2; 6;
  node [shape = circle];

  start [shape = point];
  start -> "&";
  
  "&" -> 0 [label = "E"];
  0 -> 1 [label = "b"];
  1 -> 0 [label = "b"];
  0 -> 0 [label = "а,c"];
  1 -> 1 [label = "c"];
  
  "&" -> 2 [label = "E"];
  2 -> 3 [label = "a"];
  3 -> 2 [label = "a"];
  2 -> 2 [label = "b,c"];
  3 -> 3 [label = "b,c"];
   
  "&" -> 4 [label = "E"];
  4 -> 5 [label = "c"];
  5 -> 6 [label = "c"];
  4 -> 4 [label = "a,b"];
  5 -> 5 [label = "a,b"];
  6 -> 6 [label = "a,b"];
}
```

### Здача 2

Язык
```math
\left\{\, w_1\, aa\, w_2 \;\middle|\;
\begin{aligned}
& w_1 = w_3 b w_4 \;\land\; w_2 = w_5 w_6 \;\land & |w_3| = |w_4| \;\land\; |w_5|_c = |w_6|_c
\end{aligned}
\right\}, \quad \Sigma = \{a,b,c\}.
```

Пересечем его с $c^+bc^+aa(a|b|c)^*$ и рассмотрим
![Шаблон](rk2_2.jpg)

#### Накачка только x
- Если самая $b$ из $x$ попадает в область накачки, то при нулевой накачки $xz_2$ выходим из языка
- Если у нас $c^{k-i}bc^{k-j}aab(сас...)aa$ и мы берем за разделитель $|w_3|$ и $|w_4|$ первую по счету слева $b$, то у нас тогда возможны два варианта: левый блок $c$ длиннее или правый блок $c$ длиннее. Если длиннее левый лок, тогда в силу $|w_3| = |w_4|$ в $|w_4|$ точно войдет как минимум одна $a$ из последующих $aa$ и следующая $aa$ (не входящая в $|w_1|$) будет только в конце из-за построения. Но тогда весь хвост до $aa$ должен принадлежать $|w_4|$, нарушается $|w_3| = |w_4|$, противоречие. Если длиннее правый хвост, то при рассмотрении первой по счету слева $b$ как разделителя нарушается $|w_3| = |w_4|$, так как все что до $aa$ попадает в $|w_4|$, противоречие.
- Если у нас $c^{k-i}bc^{k-j}aab(сас...)aa$ и мы берем за разделитель $|w_3|$ и $|w_4|$ вторую по счету слева $b$, то у нас также нарушается $|w_3| = |w_4|$, противоречие.

#### Накачка в x длины не >k и в z_1, z_2
- В слове $xz_2$ применим нулевю накачку. Тогдау нас не будет $aa$ в слове, противоречие.

Следовательно это не DCFL.

### Здача 3

<img width="549" height="166" alt="image" src="https://github.com/user-attachments/assets/988ab701-a111-4c58-9c9b-7644f8eba8dc" />

